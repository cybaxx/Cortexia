# TRIBE Modal `extract_bsv` URL (from `modal deploy` or `modal serve` output).
# After deploy, use the stable production URL (not the -dev tunnel from `modal serve`).
TRIBE_MODAL_URL=

# Optional: only if Modal web endpoint uses requires_proxy_auth=True
# TRIBE_MODAL_KEY=
# TRIBE_MODAL_SECRET=

# IFM K2 (optional for local mock)
# IFM_API_URL=
# IFM_API_KEY=

# ElevenLabs (optional)
# ELEVENLABS_API_KEY=
# ELEVENLABS_VOICE_ID=
